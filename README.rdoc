= Abletech/Deployment

This gem is for encapsulating Abletech's common deployment patterns using capistrano.

== Installation

# Gemfile

  gem 'easy-deployment', :git => "git@github.com:AbleTech/easy-deployment.git"

Then run:

  $ bundle install
  $ rails g abletech:deployment
